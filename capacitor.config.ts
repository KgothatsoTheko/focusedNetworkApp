import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.4WM-Innovations.focusNetworkApp',
  appName: 'focusNetworkApp',
  webDir: 'www'
};

export default config;
